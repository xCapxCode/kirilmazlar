import React from 'react';

/**
 * ModalContainer bileşeni, uygulama genelinde kullanılan modalları yönetir.
 * ModalContext ile entegre çalışır.
 */
const ModalContainer = () => {
  // Burada ModalContext'ten modal state'lerini alacağız
  // Şu an için boş bir container döndürüyoruz
  return null;
};

export default ModalContainer;
